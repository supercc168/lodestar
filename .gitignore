node_modules/

# OS / editor
.DS_Store
*.swp
*~

# Local IDE state
.serena/

# Legacy OMC operational state (if recreated)
.omc/

# Claude Code operational state (worktrees, local settings)
.claude/

# Isolated worktrees
.worktrees/

# Local override config (if dropped in repo for testing)
config.toml

# Stray runtime artifacts
*.pid
*.log

# Build outputs
dist/

# GSD multi-task local repo (independent git; not part of project history)
.gsd/

# GSD stable routing dir (symlink target lives under .gsd/; local only)
.planning/

# fpd 跨模型编排任务目录(fable-plan-dsh-exec skill;local only)
.fpd/

# Legacy superpowers local runtime/artifacts
.superpowers/

# macOS junk under agents
.agents/.DS_Store
